<section class="hbox stretch">
  <%= render partial: 'aside' %>

  <!-- .vbox -->
  <section id="content">
    <section class="vbox">
      <header class="header bg-light bg-gradient b-b">
      </header>

      <section class="scrollable wrapper">
        <div class="">
          <% # Stage Display %>
          <div class="progress" style="width: 80%">
            <div class="progress-bar progress-bar-striped active" style="width: 25%; border-right: 1px solid white">
              准备期
            </div>
            <div class="progress-bar" style="width: 25%; border-right: 1px solid white">
              募集期
            </div>
            <div class="progress-bar" style="width: 25%; border-right: 1px solid white">
              起息日
            </div>
            <div class="progress-bar" style="width: 25%; border-right: 1px solid white">
              到期日
            </div>
          </div>

          <div class="row">

            <div class="col-lg-8">
              <!-- .accordion -->
              <div class="panel-group m-b" id="accordion_stage1">

                <% @stage.tasks.each_with_index do |task, task_id| %>
                  <%= render layout: 'task', locals: { number: task_id+1, accordion_id: 'accordion_stage1', content: task.name} do %>
                    <% task.steps.each_with_index do |step, step_id| %>
                      <%= render layout: 'step', locals: { number: step_id+1, content: step.name } do %>
                      <% end %>
                    <% end %>
                  <% end %>
                <% end %>
              </div>

            </div>

          </div>

        </div>
      </section>
    </section>
  </section>
  <!-- /.vbox -->
</section>

